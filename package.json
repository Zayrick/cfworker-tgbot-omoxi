{
	"name": "tgbot-omoxi",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"generate:commands": "node scripts/generate-commands.mjs",
		"predeploy": "npm run generate:commands",
		"deploy": "wrangler deploy",
		"predev": "npm run generate:commands",
		"dev": "wrangler dev",
		"prestart": "npm run generate:commands",
		"start": "wrangler dev",
		"pretest": "npm run generate:commands",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"tyme4ts": "^1.4.1"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.61.1"
	}
}
